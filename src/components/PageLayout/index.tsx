import { Card, CardProps } from '@arco-design/web-react'
import { FC } from 'react'

const PageLayout: FC<CardProps> = (props) => {
  return (
    <Card {...props}>

    </Card>
  )
}

export default PageLayout
